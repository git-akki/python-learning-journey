# Python — Environment Setup

The first step in learning Python is making sure it runs correctly on your machine.

Most systems already have Python installed, but:
- it may not be the latest version
- it may not be correctly configured for development

This section focuses on **practical setup**, not legacy systems.

---

## Check If Python Is Already Installed

Open a terminal (CMD / PowerShell / Terminal) and run:

```bash
python --version
